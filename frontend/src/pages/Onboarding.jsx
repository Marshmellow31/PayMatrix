import { useState } from 'react';
import { motion } from 'framer-motion';
import { ArrowRight, Check, Fingerprint, ScanLine, ShieldCheck, Sparkles, Users } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import OnboardingShell from '../components/onboarding/OnboardingShell.jsx';
import TrialWorkspace from '../components/onboarding/TrialWorkspace.jsx';
import FeatureShowcase from '../components/onboarding/FeatureShowcase.jsx';
import { markOnboardingSeen } from '../hooks/useOnboardingState.js';

const Onboarding = () => {
  const navigate = useNavigate();
  const [step, setStep] = useState(0);
  const [featureIndex, setFeatureIndex] = useState(0);
  const [expenseAdded, setExpenseAdded] = useState(false);
  const finish = (path = '/login') => { markOnboardingSeen(); navigate(path); };
  const next = () => setStep((current) => Math.min(current + 1, 3));
  const back = () => setStep((current) => Math.max(current - 1, 0));

  return <OnboardingShell step={step} totalSteps={4} onBack={back} onSkip={() => finish()} onSignIn={() => finish()}>
    {step === 0 && <motion.div initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} className="grid w-full max-w-6xl gap-12 lg:grid-cols-[1fr_0.9fr] lg:items-center"><div className="max-w-2xl"><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[0.04] px-3 py-1.5 text-[10px] font-black uppercase tracking-[0.18em] text-white/55"><span className="h-1.5 w-1.5 rounded-full bg-emerald-300 shadow-[0_0_12px_rgba(110,231,183,0.8)]" /> Shared expenses, without the awkward math</div><h1 className="font-manrope text-5xl font-black leading-[0.94] tracking-[-0.06em] sm:text-7xl">Know what’s fair.<br /><span className="text-white/35">Keep it friendly.</span></h1><p className="mt-7 max-w-lg text-base leading-7 text-white/50">PayMatrix turns group expenses into a clear, shared ledger — from the first receipt to the final settlement.</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><button onClick={next} className="inline-flex items-center justify-center gap-2 rounded-full bg-white px-6 py-4 text-xs font-black uppercase tracking-[0.14em] text-black shadow-[0_10px_35px_rgba(255,255,255,0.12)] transition hover:scale-[1.02]">Try the interactive demo <ArrowRight size={16} /></button><button onClick={() => finish()} className="inline-flex items-center justify-center gap-2 rounded-full border border-white/10 px-6 py-4 text-xs font-black uppercase tracking-[0.14em] text-white/60 transition hover:border-white/25 hover:text-white">Sign in now</button></div><div className="mt-8 flex flex-wrap gap-x-5 gap-y-2 text-[11px] font-bold text-white/30"><span className="inline-flex items-center gap-2"><Fingerprint size={14} /> Your data stays yours</span><span className="inline-flex items-center gap-2"><Users size={14} /> Built for real groups</span></div></div><div className="relative mx-auto w-full max-w-md"><div className="absolute inset-8 rounded-full bg-white/[0.06] blur-3xl" /><motion.div initial={{ rotate: -5, y: 12 }} animate={{ rotate: 3, y: 0 }} transition={{ duration: 1, ease: 'easeOut' }} className="relative rounded-[2rem] border border-white/10 bg-[#171717]/90 p-5 shadow-[0_30px_100px_rgba(0,0,0,0.5)]"><div className="flex items-center justify-between border-b border-white/[0.08] pb-4"><div><p className="text-xs font-black uppercase tracking-[0.15em] text-white/30">This month</p><p className="mt-1 text-3xl font-black tracking-tight">₹4,860</p></div><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-emerald-300/10 text-emerald-200"><Check size={21} /></div></div><div className="space-y-3 py-5"><div className="flex items-center justify-between rounded-xl bg-white/[0.04] p-3"><span className="text-xs text-white/55">Goa Weekend</span><span className="text-xs font-black text-emerald-300">+₹420</span></div><div className="flex items-center justify-between rounded-xl bg-white/[0.04] p-3"><span className="text-xs text-white/55">Dinner receipt · AI scanned</span><span className="text-xs font-black text-white/70">₹2,460</span></div><div className="flex items-center justify-between rounded-xl bg-white/[0.04] p-3"><span className="text-xs text-white/55">Settlement plan</span><span className="text-xs font-black text-white/70">1 payment</span></div></div><div className="flex items-center gap-2 rounded-xl border border-emerald-300/15 bg-emerald-300/[0.06] p-3 text-[11px] text-emerald-100/70"><Sparkles size={14} className="text-emerald-300" /> Everyone sees the same answer.</div></motion.div></div></motion.div>}
    {step === 1 && <TrialWorkspace expenseAdded={expenseAdded} onAddExpense={() => setExpenseAdded(true)} onContinue={next} />}
    {step === 2 && <FeatureShowcase index={featureIndex} onChange={setFeatureIndex} onContinue={next} />}
    {step === 3 && <motion.div initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} className="w-full max-w-4xl"><div className="mx-auto max-w-xl text-center"><div className="mx-auto mb-5 flex h-14 w-14 items-center justify-center rounded-2xl bg-white text-black shadow-[0_0_40px_rgba(255,255,255,0.15)]"><ShieldCheck size={25} /></div><h1 className="font-manrope text-4xl font-black tracking-[-0.05em] sm:text-6xl">Your next group<br /><span className="text-white/35">starts here.</span></h1><p className="mt-5 text-sm leading-7 text-white/50">No long setup. Choose how you want to begin and we’ll take you straight to the useful part.</p></div><div className="mt-10 grid gap-3 sm:grid-cols-2"><button onClick={() => finish('/login?intent=create-group')} className="group rounded-[1.5rem] border border-white/15 bg-white p-6 text-left text-black transition hover:-translate-y-1 hover:shadow-[0_20px_50px_rgba(255,255,255,0.12)]"><div className="flex items-center justify-between"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-black text-white"><Users size={18} /></span><ArrowRight size={18} className="transition group-hover:translate-x-1" /></div><p className="mt-8 text-lg font-black">Create a new group</p><p className="mt-2 text-xs leading-5 text-black/55">Start a trip, event, household, or anything you share.</p></button><button onClick={() => finish('/login?intent=join-group')} className="group rounded-[1.5rem] border border-white/10 bg-white/[0.04] p-6 text-left transition hover:-translate-y-1 hover:border-white/25 hover:bg-white/[0.08]"><div className="flex items-center justify-between"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-white/[0.1] text-white"><ScanLine size={18} /></span><ArrowRight size={18} className="text-white/50 transition group-hover:translate-x-1 group-hover:text-white" /></div><p className="mt-8 text-lg font-black">Join an existing group</p><p className="mt-2 text-xs leading-5 text-white/40">Have an invite? We’ll get you into the right ledger.</p></button></div><div className="mt-8 text-center"><button onClick={() => finish()} className="text-xs font-bold text-white/40 underline decoration-white/20 underline-offset-4 transition hover:text-white">I’ll sign in and explore first</button></div></motion.div>}
  </OnboardingShell>;
};

export default Onboarding;
